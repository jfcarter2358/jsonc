{
    // this is another comment
    // configuration values for the moc-translator startup process
    "translator": {
        // oauth configuration values
        /* a c-style block comment */
        ; here is an algol comment too
        "oauth": {
            /** antoher type of c-style block comment
            **/
            <!-- html style comment -->
            <!-- multiline html comment
            -->
            # this is a python comment
            """
            a multiline python comment
            """
            '''
            another multiline python comment
            '''
            "AUTH_AUTHORIZATION_URI": "https://localhost:9031/as/authorization.oauth2", ; this is an algol comment
            "AUTH_INTROSPECTION_URI": "https://pingfederate:9031/as/introspect.oauth2", # oh look an ending python comment
            "AUTH_ISSUER_URI": "https://pingfederate:9031",
            "AUTH_JWK_SET_URI": "https://pingfederate:9031/pf/JWKS",
            "AUTH_JWT_ISSUER_URI": "http://uaa:9090/uaa/oauth/token",
            "AUTH_TOKEN_URI": "https://pingfederate:9031/as/token.oauth2",
            "AUTH_USER_INFO_URI": "https://pingfederate:9031/idp/userinfo.openid"
        }
    }
}